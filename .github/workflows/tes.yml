name: ubuntu ffmpeg

on:
  workflow_dispatch:

jobs:
 build:
  runs-on: ubuntu-latest

    steps:
      - name: Print the input name to STDOUT
        run: sudo apt install ffmpeg -y
      - name: check ffmpeg
        run: ffmpeg
