name: ubuntu ffmpeg

on:
  workflow_dispatch:

jobs:
 build:
  runs-on: ubuntu-latest

    steps:
      - name: install ffmpeg
        run: sudo apt install ffmpeg -y
      - name: check ffmpeg
        run: ffmpeg
